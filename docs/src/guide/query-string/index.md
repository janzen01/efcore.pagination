---
head:
  - - meta
    - http-equiv: refresh
      content: '0; url=/efcore.pagination/reference/query-string/'
  - - meta
    - name: robots
      content: noindex
---

# This page has moved

The query-string contract now lives under **Reference**:
[Query-string contract](/reference/query-string/).

You should have been redirected already. This address stays published because it ships inside the `10.0.0`
package READMEs on nuget.org, which are rendered against that version forever — see
`docs/scripts/verify-frozen-urls.mjs`.
