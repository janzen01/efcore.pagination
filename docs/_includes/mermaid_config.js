/* Mermaid options, interpolated raw into `var config = … ;` by the theme's components/mermaid.html.
   Must be a block comment: the theme's layout runs the page through vendor/compress.html, which joins
   the whole document onto one line, so a // comment would swallow the rest of the script.
   theme: "dark" matches color_scheme: dark — the diagrams style their own nodes (classDef fill:#512BD4,
   color:#fff) but leave the add-on nodes at fill:none, whose labels would otherwise take mermaid's
   light-theme dark text on this page background. Delete this file if the scheme goes back to light. */
{
  theme: "dark"
}
